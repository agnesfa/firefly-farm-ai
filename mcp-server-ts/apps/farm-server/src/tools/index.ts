// Farm server app-level tools - barrel export
// Add any tools that live directly in the app (not in a plugin) here.
// Generally prefer putting tools in plugins/farm-plugin/ instead.

import type { Tool } from '@fireflyagents/mcp-server-core';

export const appTools: Tool[] = [];
